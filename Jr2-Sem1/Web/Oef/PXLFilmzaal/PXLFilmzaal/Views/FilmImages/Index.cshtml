@model IEnumerable<PXLFilmzaal.Models.Data.FilmImage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FilmImageData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilmImageName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FilmImageData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FilmImageName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FilmImageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FilmImageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FilmImageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
